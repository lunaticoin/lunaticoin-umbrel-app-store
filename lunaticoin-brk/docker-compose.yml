version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: ${APP_LUNATICOIN_BRK_IP}
      APP_PORT: ${APP_LUNATICOIN_BRK_PORT}
      PROXY_AUTH_ADD: "false"

  web:
    image: ghcr.io/lunaticoin/brk:v0.2.2@sha256:5eb9cb11855de719be8ea47c766ac85cb24a28fe373bac0236af480866dbed28
    init: true
    restart: on-failure
    stop_grace_period: 5m
    volumes:
      - ${APP_DATA_DIR}/data:/home/brk/.brk
      - ${APP_BITCOIN_DATA_DIR}:/bitcoin:ro
    command:
      - --bitcoindir
      - /bitcoin
      - --blocksdir
      - /bitcoin/blocks
      - --brkdir
      - /home/brk/.brk
      - --rpcconnect
      - "${APP_BITCOIN_NODE_IP}"
      - --rpcport
      - "${APP_BITCOIN_RPC_PORT}"
      - --rpcuser
      - "${APP_BITCOIN_RPC_USER}"
      - --rpcpassword
      - "${APP_BITCOIN_RPC_PASS}"
    networks:
      default:
        ipv4_address: ${APP_LUNATICOIN_BRK_IP}
